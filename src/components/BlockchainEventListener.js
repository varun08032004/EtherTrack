import React from 'react';
import './BlockchainEventListener.css';

const BlockchainEventListener = () => {
    return (
        <div className="blockchain-event-listener">
            <h1>Blockchain Event Listener</h1>
            <p>Listen to and manage blockchain events.</p>
        </div>
    );
};

export default BlockchainEventListener;
